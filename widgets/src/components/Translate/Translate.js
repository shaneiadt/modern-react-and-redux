import React, { useState } from 'react';

export const Translate = ({ options, lang }) => {
    const [lang, setLang] = useState("en");
    
    return (
        <div>
            Translate
        </div>
    );
}